import { createTheme } from "@mui/material";

export const appTheme = createTheme({
  palette: {
    background: {
      default: "#30292F",
      paper: "#50454E",
    },
    primary: {
      main: "#DE0D92",
    },
    secondary: {
      main: "#FCB9B2",
    },
    text: {
      primary: "#FFF",
    },
  },
});
